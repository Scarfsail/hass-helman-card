import { css } from "lit-element"
export const sharedStyles = css`
    .units {
        font-size: 70%;
        color: var(--secondary-text-color);
    }     
    .clickable {
        cursor: pointer;
    }     
`